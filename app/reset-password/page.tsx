"use client";
import { useState } from "react";
import { createClient } from "@/lib/supabase/client";

export default function ResetPasswordPage(){const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [message,setMessage]=useState("");const [busy,setBusy]=useState(false);const submit=async()=>{if(password.length<10||!/[A-Z]/.test(password)||!/[a-z]/.test(password)||!/[0-9]/.test(password)){setMessage("Use at least 10 characters with uppercase, lowercase, and a number.");return}if(password!==confirm){setMessage("The passwords do not match.");return}setBusy(true);const supabase=createClient();const {error}=await supabase.auth.updateUser({password});setBusy(false);if(error)setMessage(error.message);else{setMessage("Password updated. You can now sign in.");setTimeout(()=>location.replace("/?auth=login"),1500)}};return <main className="standalone-auth"><section className="auth-card"><span className="kicker">SECURE ACCOUNT RECOVERY</span><h1>Create a new password</h1><p>Enter a strong password for your LUMA account.</p><label>New password <b className="required">*</b><input type="password" autoComplete="new-password" value={password} onChange={event=>setPassword(event.target.value)}/></label><label>Confirm password <b className="required">*</b><input type="password" autoComplete="new-password" value={confirm} onChange={event=>setConfirm(event.target.value)}/></label>{message&&<em className="form-error">{message}</em>}<button className="button auth-submit" disabled={busy} onClick={submit}>{busy?"Updating…":"Update password"}</button></section></main>}
