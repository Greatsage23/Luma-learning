"use client";import { useState } from "react";import { Dashboard,type Role } from "@/app/page";
export default function PortalClient({role,passwordChangeRequired}:{role:Role;passwordChangeRequired:boolean}){const [required,setRequired]=useState(passwordChangeRequired);const [password,setPassword]=useState("");const [confirm,setConfirm]=useState("");const [message,setMessage]=useState("");const logout=async()=>{await fetch("/api/auth/logout",{method:"POST"});location.replace("/")};const change=async()=>{const res=await fetch("/api/account/change-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password,confirmPassword:confirm})});const data=await res.json();if(res.ok){setRequired(false);setMessage("")}else setMessage(data.error)};return <>{required&&<div className="security-overlay"><div><span>SECURITY REQUIRED</span><h2>Create a new password</h2><p>Your temporary password must be replaced before you continue.</p><input type="password" placeholder="New strong password" value={password} onChange={e=>setPassword(e.target.value)}/><input type="password" placeholder="Confirm new password" value={confirm} onChange={e=>setConfirm(e.target.value)}/>{message&&<em>{message}</em>}<button onClick={change}>Change password securely</button></div></div>}<Dashboard role={role} logout={logout}/></>}
