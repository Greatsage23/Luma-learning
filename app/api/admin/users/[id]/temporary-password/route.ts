import { randomBytes } from "node:crypto";import { NextResponse } from "next/server";import { requireRole, supabaseFetch } from "@/lib/supabase";
export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const session=await requireRole(["administrator"]);if(!session)return NextResponse.json({error:"Forbidden"},{status:403});const {id}=await params;const password=`Lu!${randomBytes(8).toString("base64url")}7`;const update=await supabaseFetch(`/auth/v1/admin/users/${id}`,{method:"PUT",body:JSON.stringify({password})},undefined,true);if(!update.ok)return NextResponse.json({error:"Password reset failed."},{status:update.status});await supabaseFetch(`/rest/v1/profiles?id=eq.${id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({status:"active",password_changed_at:null,updated_at:new Date().toISOString()})},undefined,true);await supabaseFetch("/rest/v1/audit_logs",{method:"POST",headers:{Prefer:"return=minimal"},body:JSON.stringify({actor_id:session.user.id,action:"temporary_password_issued",affected_record:id})},undefined,true);return NextResponse.json({temporaryPassword:password})}
