import { randomBytes } from "node:crypto";import { NextResponse } from "next/server";import { requireRole, supabaseFetch } from "@/lib/supabase";
const temporaryPassword=()=>`Lu!${randomBytes(8).toString("base64url")}9`;
export async function GET(request:Request){const session=await requireRole(["administrator"]);if(!session)return NextResponse.json({error:"Forbidden"},{status:403});const query=new URL(request.url).searchParams;const role=query.get("role");const status=query.get("status");const search=query.get("search");let path="/rest/v1/profiles?select=*,teacher_assignments(subjects,classes)&order=created_at.desc&limit=100";if(role)path+=`&role=eq.${encodeURIComponent(role)}`;if(status)path+=`&status=eq.${encodeURIComponent(status)}`;if(search)path+=`&or=(full_name.ilike.*${encodeURIComponent(search)}*,email.ilike.*${encodeURIComponent(search)}*,username.ilike.*${encodeURIComponent(search)}*)`;const result=await supabaseFetch(path,{},session.token);return NextResponse.json(await result.json(),{status:result.status})}
export async function POST(request:Request){const session=await requireRole(["administrator"]);if(!session)return NextResponse.json({error:"Forbidden"},{status:403});try{const body=await request.json();const {fullName,email,phone,staffId,username,subjects,classes,status="password_change_required"}=body;if(!fullName||!email||!phone||!staffId||!username)return NextResponse.json({error:"Complete every required teacher field."},{status:400});const password=temporaryPassword();const created=await supabaseFetch("/auth/v1/admin/users",{method:"POST",body:JSON.stringify({email,password,email_confirm:true,user_metadata:{full_name:fullName,phone,staff_id:staffId,username,role:"teacher"}})},undefined,true);const user=await created.json();if(!created.ok)return NextResponse.json({error:user.msg||"Email, username, or staff ID already exists."},{status:409});await supabaseFetch(`/rest/v1/profiles?id=eq.${user.id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({full_name:fullName,email,phone,staff_id:staffId,username,role:"teacher",status,created_by:session.user.id})},undefined,true);if((subjects?.length||classes?.length))await supabaseFetch("/rest/v1/teacher_assignments",{method:"POST",headers:{Prefer:"return=minimal"},body:JSON.stringify([{teacher_id:user.id,subjects:subjects||[],classes:classes||[],assigned_by:session.user.id}])},undefined,true);await supabaseFetch("/rest/v1/audit_logs",{method:"POST",headers:{Prefer:"return=minimal"},body:JSON.stringify({actor_id:session.user.id,action:"teacher_account_created",affected_record:user.id,new_value:{email,staffId,username,subjects,classes}})},undefined,true);return NextResponse.json({id:user.id,temporaryPassword,requiresPasswordChange:true},{status:201})}catch{return NextResponse.json({error:"Unable to create the teacher account."},{status:500})}}
