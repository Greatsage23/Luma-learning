import { NextResponse } from "next/server";
import { currentSession,supabaseFetch } from "@/lib/supabase";
export async function GET(){const session=await currentSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const response=await supabaseFetch(`/rest/v1/notifications?select=*&user_id=eq.${session.user.id}&order=created_at.desc&limit=50`,{},session.token);return NextResponse.json(await response.json(),{status:response.status})}
export async function PATCH(request:Request){const session=await currentSession();if(!session)return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await request.json();const response=await supabaseFetch(`/rest/v1/notifications?id=eq.${Number(id)}&user_id=eq.${session.user.id}`,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({read_at:new Date().toISOString()})},session.token);return response.ok?NextResponse.json({ok:true}):NextResponse.json({error:"Unable to update notification."},{status:response.status})}
