import { integer, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";

const timestamps = { createdAt: integer("created_at", { mode: "timestamp" }).notNull(), updatedAt: integer("updated_at", { mode: "timestamp" }).notNull() };
export const users = sqliteTable("users", { id:text("id").primaryKey(), email:text("email").notNull().unique(), passwordHash:text("password_hash"), role:text("role",{enum:["student","teacher","administrator"]}).notNull(), status:text("status",{enum:["pending","active","suspended"]}).notNull().default("active"), name:text("name").notNull(), ...timestamps });
export const studentProfiles = sqliteTable("student_profiles", { userId:text("user_id").primaryKey().references(()=>users.id), studentNumber:text("student_number").notNull().unique(), classLevel:text("class_level").notNull(), school:text("school"), guardianName:text("guardian_name"), guardianPhone:text("guardian_phone") });
export const teacherProfiles = sqliteTable("teacher_profiles", { userId:text("user_id").primaryKey().references(()=>users.id), employeeNumber:text("employee_number").notNull().unique(), approvedBy:text("approved_by").references(()=>users.id), approvedAt:integer("approved_at",{mode:"timestamp"}) });
export const academicYears = sqliteTable("academic_years", { id:text("id").primaryKey(), name:text("name").notNull(), startsAt:integer("starts_at",{mode:"timestamp"}).notNull(), endsAt:integer("ends_at",{mode:"timestamp"}).notNull() });
export const subjects = sqliteTable("subjects", { id:text("id").primaryKey(), name:text("name").notNull(), slug:text("slug").notNull().unique() });
export const courses = sqliteTable("courses", { id:text("id").primaryKey(), subjectId:text("subject_id").notNull().references(()=>subjects.id), teacherId:text("teacher_id").notNull().references(()=>users.id), classLevel:text("class_level").notNull(), title:text("title").notNull(), description:text("description").notNull(), accessLevel:text("access_level",{enum:["free","premium"]}).notNull(), publishedAt:integer("published_at",{mode:"timestamp"}), ...timestamps });
export const modules = sqliteTable("modules", { id:text("id").primaryKey(), courseId:text("course_id").notNull().references(()=>courses.id), title:text("title").notNull(), position:integer("position").notNull(), unlockAt:integer("unlock_at",{mode:"timestamp"}) });
export const lessons = sqliteTable("lessons", { id:text("id").primaryKey(), moduleId:text("module_id").notNull().references(()=>modules.id), title:text("title").notNull(), body:text("body").notNull(), objectives:text("objectives",{mode:"json"}), estimatedMinutes:integer("estimated_minutes").notNull(), position:integer("position").notNull(), accessLevel:text("access_level",{enum:["free","premium"]}).notNull(), prerequisiteLessonId:text("prerequisite_lesson_id"), ...timestamps });
export const resources = sqliteTable("resources", { id:text("id").primaryKey(), lessonId:text("lesson_id").notNull().references(()=>lessons.id), storageKey:text("storage_key").notNull(), filename:text("filename").notNull(), mimeType:text("mime_type").notNull(), bytes:integer("bytes").notNull(), accessLevel:text("access_level",{enum:["free","premium"]}).notNull() });
export const enrolments = sqliteTable("enrolments", { id:text("id").primaryKey(), studentId:text("student_id").notNull().references(()=>users.id), courseId:text("course_id").notNull().references(()=>courses.id), enrolledAt:integer("enrolled_at",{mode:"timestamp"}).notNull() },t=>[uniqueIndex("enrolment_student_course").on(t.studentId,t.courseId)]);
export const lessonProgress = sqliteTable("lesson_progress", { id:text("id").primaryKey(), studentId:text("student_id").notNull().references(()=>users.id), lessonId:text("lesson_id").notNull().references(()=>lessons.id), percent:integer("percent").notNull().default(0), lastPositionSeconds:integer("last_position_seconds").notNull().default(0), completedAt:integer("completed_at",{mode:"timestamp"), updatedAt:integer("updated_at",{mode:"timestamp"}).notNull() },t=>[uniqueIndex("progress_student_lesson").on(t.studentId,t.lessonId)]);
export const assessments = sqliteTable("assessments", { id:text("id").primaryKey(), courseId:text("course_id").notNull().references(()=>courses.id), title:text("title").notNull(), type:text("type").notNull(), durationMinutes:integer("duration_minutes"), attemptLimit:integer("attempt_limit").notNull().default(1), passMark:integer("pass_mark").notNull(), accessLevel:text("access_level",{enum:["free","premium"]}).notNull(), opensAt:integer("opens_at",{mode:"timestamp"}), closesAt:integer("closes_at",{mode:"timestamp"}) });
export const questions = sqliteTable("questions", { id:text("id").primaryKey(), assessmentId:text("assessment_id").notNull().references(()=>assessments.id), type:text("type").notNull(), prompt:text("prompt").notNull(), options:text("options",{mode:"json"}), answerKey:text("answer_key",{mode:"json"}), marks:integer("marks").notNull(), difficulty:text("difficulty").notNull() });
export const attempts = sqliteTable("assessment_attempts", { id:text("id").primaryKey(), assessmentId:text("assessment_id").notNull().references(()=>assessments.id), studentId:text("student_id").notNull().references(()=>users.id), status:text("status").notNull(), answers:text("answers",{mode:"json"}), score:integer("score"), feedback:text("feedback"), startedAt:integer("started_at",{mode:"timestamp"}).notNull(), submittedAt:integer("submitted_at",{mode:"timestamp"}) });
export const assignments = sqliteTable("assignments", { id:text("id").primaryKey(), courseId:text("course_id").notNull().references(()=>courses.id), title:text("title").notNull(), instructions:text("instructions").notNull(), rubric:text("rubric",{mode:"json"}), dueAt:integer("due_at",{mode:"timestamp"}).notNull(), accessLevel:text("access_level",{enum:["free","premium"]}).notNull(), groupWork:integer("group_work",{mode:"boolean"}).notNull().default(false) });
export const submissions = sqliteTable("submissions", { id:text("id").primaryKey(), assignmentId:text("assignment_id").notNull().references(()=>assignments.id), studentId:text("student_id").notNull().references(()=>users.id), status:text("status").notNull(), body:text("body"), storageKeys:text("storage_keys",{mode:"json"}), score:integer("score"), feedback:text("feedback"), submittedAt:integer("submitted_at",{mode:"timestamp"}), gradedBy:text("graded_by").references(()=>users.id), gradedAt:integer("graded_at",{mode:"timestamp"}) });
export const lessonNotes = sqliteTable("lesson_notes", { id:text("id").primaryKey(), teacherId:text("teacher_id").notNull().references(()=>users.id), courseId:text("course_id").notNull().references(()=>courses.id), week:integer("week").notNull(), storageKey:text("storage_key").notNull(), status:text("status",{enum:["draft","submitted","approved","returned","rejected"]}).notNull(), reviewComment:text("review_comment"), reviewedBy:text("reviewed_by").references(()=>users.id), reviewedAt:integer("reviewed_at",{mode:"timestamp"}), ...timestamps });
export const subscriptionPlans = sqliteTable("subscription_plans", { id:text("id").primaryKey(), name:text("name").notNull(), amountPesewas:integer("amount_pesewas").notNull(), durationDays:integer("duration_days").notNull(), scope:text("scope").notNull(), benefits:text("benefits",{mode:"json"}).notNull(), active:integer("active",{mode:"boolean"}).notNull().default(true) });
export const subscriptions = sqliteTable("subscriptions", { id:text("id").primaryKey(), studentId:text("student_id").notNull().references(()=>users.id), planId:text("plan_id").notNull().references(()=>subscriptionPlans.id), status:text("status").notNull(), startsAt:integer("starts_at",{mode:"timestamp"}), expiresAt:integer("expires_at",{mode:"timestamp"}), autoRenew:integer("auto_renew",{mode:"boolean"}).notNull().default(false), paymentId:text("payment_id") });
export const payments = sqliteTable("payments", { id:text("id").primaryKey(), reference:text("reference").notNull().unique(), studentId:text("student_id").notNull().references(()=>users.id), planId:text("plan_id").notNull().references(()=>subscriptionPlans.id), provider:text("provider").notNull(), providerReference:text("provider_reference"), amountPesewas:integer("amount_pesewas").notNull(), currency:text("currency").notNull().default("GHS"), status:text("status").notNull(), channel:text("channel"), payerEmail:text("payer_email"), verifiedAt:integer("verified_at",{mode:"timestamp"}), ...timestamps });
export const webhookEvents = sqliteTable("webhook_events", { id:text("id").primaryKey(), provider:text("provider").notNull(), providerEventId:text("provider_event_id").notNull(), signatureValid:integer("signature_valid",{mode:"boolean"}).notNull(), payloadHash:text("payload_hash").notNull(), processedAt:integer("processed_at",{mode:"timestamp"}), createdAt:integer("created_at",{mode:"timestamp"}).notNull() },t=>[uniqueIndex("webhook_provider_event").on(t.provider,t.providerEventId)]);
export const auditLogs = sqliteTable("audit_logs", { id:text("id").primaryKey(), actorId:text("actor_id").references(()=>users.id), action:text("action").notNull(), entityType:text("entity_type").notNull(), entityId:text("entity_id"), reason:text("reason"), beforeHash:text("before_hash"), afterHash:text("after_hash"), ipHash:text("ip_hash"), createdAt:integer("created_at",{mode:"timestamp"}).notNull() });
